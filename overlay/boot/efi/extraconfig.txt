# uConsole CM5 Configuration

# Use the uConsole-specific device tree
device_tree=merged-clockworkpi.dtb

# Core Overlays
dtoverlay=rp1-i2c1-fix
dtoverlay=usb-vbus-hog
dtoverlay=dwc2,dr_mode=host

# uConsole Specifics
dtoverlay=clockworkpi-uconsole-cm5
dtoverlay=uconsole-audio
dtoverlay=uconsole-sd-alt

# Enable GPU Acceleration (V3D) with increased CMA for compositors
dtoverlay=vc4-kms-v3d-pi5,cma-512